package com.chat.server;

import com.chat.common.Command;

/**
 * Handles the logic for processing incoming commands from clients.
 * (Currently a placeholder for future implementation).
 */
public class CommandProcessor {
    /**
     * Parses and processes a single line of input from a client.
     * 
     * @param line The raw string message received from the client.
     */
    public void process(String line) {
        // Placeholder for future command processing logic
        // This could use the Command enum to determine actions
    }
}

